{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"KEML","text":"<p>KEML \u2014 Actions over abstractions. Enhance HTML with expressive, declarative attributes that connect your frontend directly to your server logic.</p> <p> </p> <p>Warning</p> <p>JavaScript is disabled.</p> <p>Thou shalt not pass.</p> <p>But, seriously, lots of things will be broken. Go enable it first!</p> <pre><code>&lt;button\n  on:click=\"submitDiv\"\n&gt;\n  Click me!\n&lt;/button&gt;\n\n&lt;!-- I promise, I am a form element ;) --&gt;\n&lt;div\n  on=\"submitDiv\"\n  name=\"weirdDiv\"\n  value=\"hello\"\n  result=\"serverSaysHi\"\n&gt;&lt;/div&gt;\n\n&lt;!-- I will be overwritten --&gt;\n&lt;p\n  render=\"serverSaysHi\"\n  position=\"replaceWith\"\n&gt;&lt;/p&gt;\n\n&lt;!-- yep, nothing special here --&gt;\n&lt;title render=\"serverSaysHi\"&gt;&lt;/title&gt;\n</code></pre> <p>Curious? Confused? - Great! There is plenty of other pages to read \ud83d\ude00</p>"},{"location":"installation/","title":"Installation","text":"<p>KEML is designed to be ultra simple to use \u2014 there is no programmatic API, no JavaScript exports, and no setup or configuration required.</p> <p>First, install the official KEML extension from the VS Code Marketplace if you haven't already.</p> <p>Then, simply include the library via CDN in your HTML:</p> <pre><code>&lt;script src=\"https://unpkg.com/keml\"&gt;&lt;/script&gt;\n</code></pre> <p>Alternatively, if you prefer to manage dependencies locally or bundle KEML with your project, install it using npm:</p> <pre><code>npm install keml\n</code></pre> <p>The compiled bundle, <code>keml.js</code>, will be available at the root of the installed package.</p>"},{"location":"installation/#using-keml-with-other-libraries-and-frameworks","title":"Using KEML with Other Libraries and Frameworks","text":"<p>KEML observes DOM changes, so you don\u2019t need to do anything special to integrate it with your app.</p> <p>Whenever your application dynamically creates or updates elements, KEML will seamlessly handle them just like elements rendered on the server.</p> <p>This makes KEML compatible with virtually any front-end framework or custom JavaScript code, without any extra setup.</p>"},{"location":"installation/#demos","title":"Demos","text":"<p>All interactive demos in this documentation run on the real KEML runtime.</p> <p>Each demo is split into three tabs:</p> <ul> <li>HTML \u2014 the client program</li> <li>Server \u2014 the server program</li> <li>Result \u2014 the running application produced by executing both programs</li> </ul> <p>The HTML and Server tabs contain the actual programs used to produce the Result tab.</p> <p>You can also explore the examples folder on GitHub.</p>"},{"location":"logging/","title":"Logging","text":"<p>Sometimes, the information available statically, just by reading the HTML, may not be enough, and you may need to peek at the runtime value of an important variable.</p> <p>With a well-designed API, such situations should be rare. But the real world is messy, and they cannot be avoided entirely.</p> <p>This is where the dedicated <code>log</code> attribute comes in.</p> <p>Most of the time, it does nothing and is generally harmless to add, even to every single element. However, it is still a debugging flag and, from a purely aesthetic point of view, should not be left enabled in production.</p> <p>All it ever does is log information to the JavaScript console that the element deems important at a particular moment in time.</p> <p>Info</p> <p>Currently, this attribute only logs event objects in the presence of an event: modifier, right before performing the matching for it. However, the <code>log</code> attribute itself is meant to be general-purpose, and its functionality may be extended in the future.</p> HTML Result <pre><code>&lt;h4 class=\"mt0\"&gt;Type in the field and watch events appear in the console.&lt;/h4&gt;\n\n&lt;input\n  type=\"text\"\n  class=\"input\"\n  on:keyup=\"logTest\"\n  event:keyup\n  log\n&gt;\n</code></pre> https://www.log-example.com/"},{"location":"motivation/","title":"Motivation","text":"<p>Simple systems are easy to maintain. Large ones are not.</p> <p>As applications grow, complexity accumulates. Logic spreads across layers, state has to be coordinated, and small changes become harder to reason about.</p> <p>At some point, many projects reach a level of complexity where rewriting feels like the only viable option. The rewrite works, briefly, because the system is small again. Then it grows, and the same repeats.</p>"},{"location":"motivation/#in-comes-hateoas","title":"In comes HATEOAS","text":"<p>KEML (and HTMX) are often misread as just another way to query a server.</p> <p>That misses the actual shift:</p> <ul> <li>application data and logic exist only on the server</li> <li>UI is generated from server responses</li> <li>there is no UI state at all</li> </ul> <p>KEML removes the sources of complexity instead of managing them \u2014 frontend complexity does not exist, because the frontend itself doesn't.</p> <p>The server can still be complex, but its model is inherently constrained: each request starts from nothing, produces a response, and terminates \u2014 every interaction is finite.</p>"},{"location":"motivation/#so-whats-wrong-with-htmx","title":"So, what's wrong with HTMX?","text":"<p>Nothing. Nothing at all.</p> <p>But, it is different enough that no amount of plugins can turn it into KEML.</p>"},{"location":"motivation/#defining-characteristic","title":"Defining characteristic","text":"<p>What makes KEML special is the way that elements communicate with each other - they simply do not.</p> <p>There are no selectors and no way to reference another element.</p> <p>Each element only configures itself, its own inputs and outputs, and does not know where the inputs are coming from or where the outputs go.</p> <p>Every element in KEML conceptually boils down to the following type signature: <code>Array&lt;(action: string) =&gt; string[]&gt;</code>, a collection of functions that each take one action string and produce zero or more action strings.</p> <p>Thus, interactions are naturally many-to-many.</p> <p>Still confused? - Awesome \ud83d\ude00</p>"},{"location":"news/","title":"News","text":""},{"location":"news/#v34","title":"v3.4","text":"<p>Added:</p> <ul> <li><code>failure</code> and <code>discover</code> event types</li> </ul> <p>Changed:</p> <ul> <li>unified internal attribute resolution</li> <li><code>name</code> + <code>value</code> attribute pairs now work at any nesting level</li> <li>documentation rewrite</li> </ul>"},{"location":"news/#v33","title":"v3.3","text":"<p>Added:</p> <ul> <li>SSE</li> <li>scroll</li> <li>log</li> </ul>"},{"location":"news/#vs-code-extension","title":"VS Code Extension","text":"<p>KEML now has an official VS Code extension.</p>"},{"location":"news/#v32","title":"v3.2","text":"<p>Fixed:</p> <ul> <li>removed auto-install for python dependencies</li> </ul>"},{"location":"news/#v31","title":"v3.1","text":"<p>Added:</p> <ul> <li>parent handler</li> </ul>"},{"location":"news/#v3","title":"v3","text":"<p>Added:</p> <ul> <li>automatic tracking for all DOM changes</li> <li>event: modifier</li> </ul> <p>Breaking:</p> <ul> <li>removed special actions</li> </ul>"},{"location":"reference/","title":"Reference","text":"Name Applied With Description <code>action</code>, <code>get</code>, <code>href</code>, <code>src</code> <code>on</code> / <code>sse</code> endpoint override <code>behavior</code> <code>scroll</code> scroll animation <code>credentials</code> <code>on</code> / <code>sse</code> credentials for cross-domain communication <code>debounce</code> <code>on</code> debounce the <code>on</code> feature <code>delete</code>, <code>post</code>, <code>put</code> <code>on</code> / <code>sse</code> endpoint and method override <code>error</code> <code>on</code> result action output channel <code>event:&lt;event&gt;</code> <code>on:&lt;event&gt;</code> block an event action output channel <code>h-&lt;header&gt;</code> <code>on</code> request header <code>if:error</code>, <code>if:loading</code> <code>on</code> state action output channel <code>if:intersects</code>, <code>if:invalid</code>, <code>if:value</code> * state action output channel <code>if</code> * state action input channel <code>key</code> * element identification for the DOM patcher <code>left</code> <code>scroll</code> scroll horizontal offset <code>log</code> * enables logging <code>method</code> <code>on</code> method override <code>on:&lt;event&gt;</code> * event action output channel <code>on</code> * event action input channel for sending requests <code>once</code> <code>on</code> <code>on</code> removed after first use <code>position</code> <code>render</code> render position <code>redirect</code> <code>on</code> send redirects instead of requests <code>relative</code> <code>scroll</code> use relative scroll offsets <code>render</code> * result action input channel <code>reset</code> * event action input channel for resetting elements <code>result</code> <code>on</code> / <code>sse</code> result action output channel <code>scroll</code> * event action input channel for scrolling elements <code>sse</code> * SSE event type <code>throttle</code> <code>on</code> throttle the <code>on</code> feature <code>top</code> <code>scroll</code> scroll vertical offset <code>x-&lt;attribute&gt;</code> <code>if</code> state condition reactive switch"},{"location":"sse/","title":"SSE","text":"<p>Info</p> <p>SSE connections are inherently limited across browser tabs on the same domain. KEML does everything it can to minimize them while remaining spec compliant. But you are still advised to limit them as much as possible. Reuse the same endpoint for SSE communication and only change the event type.</p> <p>SSE, or Server-sent Events, is a method of sending results from the server without first being prompted by a request. The server decides entirely on its own when and what to send.</p> <p>Think of it as a lighter and simpler alternative to WebSockets that can only send data in one direction \u2014 from server to client. And given that we have always been able to send data from client to server using normal requests, this already satisfies the need for full bi-directional communication.</p> <p>Polling also works, but it is like spamming your server with constant \"Are we there yet?\" queries. You have to balance between sending them too often and getting a \"No!\" every time, or sending them too infrequently and accepting that you might be displaying stale data sometimes.</p> <ul> <li>the attribute takes an event type sent by the server as a value, and if   omitted, defaults to <code>message</code></li> <li>endpoint resolution and override rules are the same as with   on</li> <li>the HTTP method is always <code>GET</code> and cannot be overridden</li> <li>the <code>credentials</code> attribute behaves the same</li> <li>result actions and events behave the same, except there is no concept of an   invalid status code</li> </ul> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;Inception clock&lt;/h4&gt;\n\n&lt;p&gt;Counting time elapsed since the initial KEML release.&lt;/p&gt;\n\n&lt;p&gt;Fully controlled and updated by the server. No polling.&lt;/p&gt;\n\n&lt;div\n  class=\"clock\"\n  sse=\"inception-clock\"\n  src=\"/sse\"\n  result=\"sseResult\"\n  render=\"sseResult\"\n&gt;&lt;/div&gt;\n</code></pre> <pre><code>{{\n  const segment = ([value, label]) =&gt;\n    \"&lt;span&gt;\" + value + \"&lt;/span&gt;&lt;span&gt;\" + label + \"&lt;/span&gt;\";\n\n  const clock = () =&gt; server.timeSince(1716062717000).map(segment).join(\"\");\n\n  const tick = () =&gt; server.dispatchEvent(\"inception-clock\", clock());\n\n  server.addIntervalId(setInterval(tick, 1000));\n}}\n</code></pre> Inception clock <p>Counting time elapsed since the initial KEML release.</p> <p>Fully controlled and updated by the server. No polling.</p>"},{"location":"taxonomy/","title":"Taxonomy","text":"<p>This page provides centralized definitions for KEML-specific terminology.</p>"},{"location":"taxonomy/#action","title":"action","text":"<p>An action in KEML is a global name, loosely similar to CSS class names as used in HTML.</p> <p>It does not perform any work and is not owned by any element. It simply exists as a declared intent that elements may produce or consume.</p> <p>For example, <code>on:mouseover=\"foo bar\"</code> vs <code>class=\"foo bar\"</code>. <code>foo</code> and <code>bar</code> are both global identifiers in their respective systems. They are not behavior \u2014 just names. In that sense, they are the same kind of thing.</p> <p>Because actions are so simple, they can be understood by tooling. They can be statically analyzed and strictly typed.</p> <p>Actions can be either used as inputs or outputs. You can only input one action at a time into a receiving attribute, but output many.</p> <p>There are 3* distinct action types in KEML:</p> <ul> <li>event actions are generated by browser events</li> <li>result actions are generated by server results</li> <li>state actions are generated by evaluating state conditions</li> <li>SSE event types could be imagined as being the forth type, in the   sense that they are used as event-like input actions (the output is on the   server). They technically are not, though. But, they are also not really   events in the same way as DOM events are either (I swear it is not as bad as   it sounds, KEML makes them really nice to work with)</li> </ul> <p>You can only consume an action of a compatible type.</p> <p>Info</p> <p>All actions are global within the page they are used on. Use unique action names when needed to prevent collisions.</p>"},{"location":"taxonomy/#state","title":"state","text":"<p>A state in KEML does not represent data.</p> <p>It is a boolean condition that an element can check on itself and output zero or more actions.</p> <p>For example, <code>if:loading=\"spinner\"</code>. <code>loading</code> is a state condition and <code>spinner</code> is a state action.</p>"},{"location":"taxonomy/#result","title":"result","text":"<p>Even though this word may appear to be used interchangeably with response, the two are not equal in meaning.</p> <p>A response is a reply to a request, while a result can also arrive completely unprompted, such as through SSE.</p> <p>All responses are results, but not all results are responses.</p> <p>Now that we're done with the formalities, lets see it in action \ud83c\udf89</p>"},{"location":"event/on-colon/","title":"on:*","text":"<p>Event actions can be defined on any element with the following pattern:</p> <p><code>on:&lt;event&gt;=\"hello KEML\"</code></p> <p>This is not an event handler in the traditional sense, because it does not imply any work being done.</p> <p>This reads like the following:</p> <ul> <li>define <code>hello</code> and <code>KEML</code> as actions of type <code>event</code></li> <li>assign them to the output channel of <code>&lt;event&gt;</code></li> </ul> <p>Event actions can then be used with the following pattern:</p> <p><code>&lt;feature&gt;=\"hello\"</code></p> <p>This reads like the following:</p> <ul> <li>assign the <code>hello</code> action of type <code>event</code> to the input channel of <code>&lt;feature&gt;</code></li> </ul> <p>Again, this does not imply any work by itself - each individual feature will decide what to do with the input.</p> <p>Info</p> <p>Both sides are declarations \u2014 KEML has no event or signal system.</p> <p>It runs in an event-driven browser, but itself is purely declarative: nothing is emitted or observed, and neither side is aware of the other.</p>"},{"location":"event/on-colon/#supported-events","title":"Supported events","text":"<p>All event types are supported by KEML, regardless of their origin \u2014 KEML makes no distinction between them:</p> <ul> <li>native events</li> <li>events defined by KEML itself</li> <li>custom events emitted by other JavaScript on an element</li> </ul>"},{"location":"event/on-colon/#keml-defined-events","title":"KEML-defined events","text":"<ol> <li><code>reveal</code> \u2014 element enters the viewport</li> <li><code>conceal</code> \u2014 element leaves the viewport</li> <li><code>navigate</code> \u2014 History API transition</li> <li><code>result</code> \u2014 successful server result</li> <li><code>failure</code> \u2014 unsuccessful server result</li> <li><code>discover</code> \u2014 the element becomes known to the system (if the <code>on:discover</code>    attribute is removed and then added back, the event will be re-emitted)</li> </ol>"},{"location":"event/on-colon/#event-modifier","title":"<code>event:</code> modifier","text":"<p>The output channel of event actions can be conditionally blocked by an <code>event:</code> modifier:</p> <p><code>event:&lt;event&gt;=\"key = value, otherKey = otherValue, truthyCheck\"</code></p> <p>This performs a shallow comparison between an event object and a specification provided in quotes.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;Type something in and press `Ctrl+Enter` \ud83d\ude0a&lt;/h4&gt;\n\n&lt;input\n  name=\"message\"\n  autocomplete=\"off\"\n  class=\"input\"\n  required\n  on:keydown=\"submitEventModifier\"\n  event:keydown=\"key = Enter, ctrlKey\"\n  on=\"submitEventModifier\"\n  src=\"/event-modifier\"\n  result=\"eventModifierResult\"\n&gt;\n\n&lt;div render=\"eventModifierResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Shortcut triggered&lt;/small&gt;\n\n&lt;p&gt;You just used your first hotkey with KEML!&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Your message:&lt;/dt&gt;\n  &lt;dd&gt;{ server.getParam(\"message\") }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre> Type something in and press `Ctrl+Enter` \ud83d\ude0a"},{"location":"event/on-colon/#parent-handler","title":"Parent handler","text":"<p>KEML does not interfere with normal browser event bubbling. This is not bubbling, but simple parent lookup.</p> <p><code>on:*</code> does not have to be set directly on the event emitting element; it can also be on any of its parents.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;\n  The button is clicked, a parent div is outputting the actions, but then the\n  button itself is still handling them anyway \ud83d\ude05.\n&lt;/h4&gt;\n\n&lt;div on:click=\"sendFromParent\"&gt;\n  &lt;div&gt;\n    &lt;button\n      type=\"button\"\n      class=\"btn\"\n      on=\"sendFromParent\"\n      href=\"/parent-handler\"\n      result=\"parentResult\"\n    &gt;\n      click me\n    &lt;/button&gt;\n  &lt;/div&gt;\n&lt;/div&gt;\n\n&lt;div render=\"parentResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Received&lt;/small&gt;\n\n&lt;p class=\"mb0\"&gt;Click handled by a parent element.&lt;/p&gt;\n</code></pre>    The button is clicked, a parent div is outputting the actions, but then the   button itself is still handling them anyway \ud83d\ude05.         click me"},{"location":"event/on/","title":"on","text":"<p>This feature is responsible for initiating requests to the server.</p> <p>It supports the following request types:</p> <ul> <li>XHR requests, including form submissions</li> <li>full page transitions</li> <li>History API navigation</li> </ul> <p>Its only responsibility is configuring and sending the request. What happens after the request is outside the scope of this feature.</p> <p>All XHR requests include an <code>X-Requested-With</code> header set to <code>XMLHttpRequest</code>, allowing the server to distinguish XHR requests from full page navigation when needed.</p> <p>KEML sets a permanent cookie named <code>tzo</code>, containing the browser timezone after the first load of any page that includes the KEML runtime, all subsequent requests will include this cookie.</p> <p>KEML relies heavily on and benefits from the existing web stack, without overriding its semantics or behavior, including the standard browser page caching mechanisms.</p> <p>Thus, if your server is smart about sending the correct caching response headers at the correct time - that can help speed up KEML even more.</p> <p>That's right, your application's performance optimizations also live on the server \ud83e\udd2f.</p> <p>The <code>on</code> attribute is the only required attribute for an element to become capable of initiating requests to the server.</p> <p>Even though most examples here show <code>on</code> being used on the same element as <code>on:*</code>, that is only for brevity and simplicity's sake. It can appear on any number of elements at once.</p> <p><code>on</code> provides a set of optional attributes for customizing its behavior. These are covered in the sections below.</p>"},{"location":"event/on/#endpointmethod-configuration","title":"Endpoint+Method Configuration","text":"<p>The default endpoint is an empty string (<code>\"\"</code>).</p> <p>The default HTTP method is <code>GET</code>.</p> <p>Given the current URL:</p> <pre><code>https://www.example.com/some/path\n</code></pre> Configuration Result (empty) https://www.example.com/some/path/ <code>list-todo</code> or <code>./list-todo</code> https://www.example.com/some/path/list-todo/ <code>../list-todo</code> https://www.example.com/some/list-todo/ <code>/list-todo</code> https://www.example.com/list-todo/ <code>/file.html</code> https://www.example.com/file.html <p>Automatic normalization:</p> <ul> <li>paths ending in a file extension \u2192 exactly zero trailing slashes</li> <li>paths without a file extension \u2192 exactly one trailing slash</li> </ul>"},{"location":"event/on/#get-href-action-and-src","title":"<code>get</code>, <code>href</code>, <code>action</code> and <code>src</code>","text":"<p>These attributes override the default endpoint.</p> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"submitOverrideGetEndpoint\"\n  on=\"submitOverrideGetEndpoint\"\n  action=\"/endpoint-override\"\n  result=\"overrideGetEndpointResult\"\n&gt;\n  click me\n&lt;/button&gt;\n\n&lt;div render=\"overrideGetEndpointResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mv3\"&gt;Request received&lt;/small&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Endpoint:&lt;/dt&gt;\n  &lt;dd&gt;{ server.url.pathname }&lt;/dd&gt;\n  &lt;dt&gt;Method:&lt;/dt&gt;\n  &lt;dd&gt;{ server.method }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    click me"},{"location":"event/on/#post-put-and-delete","title":"<code>post</code>, <code>put</code> and <code>delete</code>","text":"<p>These attributes override both the default endpoint and the default HTTP method.</p> <p>The method they set is the same as the attribute name.</p> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"submitOverridePostEndpoint\"\n  on=\"submitOverridePostEndpoint\"\n  post=\"/endpoint-override\"\n  result=\"overridePostEndpointResult\"\n&gt;\n  click me\n&lt;/button&gt;\n\n&lt;div render=\"overridePostEndpointResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mv3\"&gt;Request received&lt;/small&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Endpoint:&lt;/dt&gt;\n  &lt;dd&gt;{ server.url.pathname }&lt;/dd&gt;\n  &lt;dt&gt;Method:&lt;/dt&gt;\n  &lt;dd&gt;{ server.method }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    click me"},{"location":"event/on/#method","title":"<code>method</code>","text":"<p>This attribute overrides just the default HTTP method. The provided value will be converted into all uppercase.</p> <p>There are no restrictions on the value \u2014 it can be anything as long as your server understands the verb.</p> <p>It has higher precedence than the attributes shown above.</p> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"submitOverrideMethod\"\n  on=\"submitOverrideMethod\"\n  put=\"/endpoint-override\"\n  method=\"custom\"\n  result=\"overrideMethodResult\"\n&gt;\n  click me\n&lt;/button&gt;\n\n&lt;div render=\"overrideMethodResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mv3\"&gt;Request received&lt;/small&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Endpoint:&lt;/dt&gt;\n  &lt;dd&gt;{ server.url.pathname }&lt;/dd&gt;\n  &lt;dt&gt;Method:&lt;/dt&gt;\n  &lt;dd&gt;{ server.method }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    click me"},{"location":"event/on/#debounce-and-throttle","title":"debounce and throttle","text":"<p>These attributes can be used to rate limit the <code>on</code> feature.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;\n  The request is only sent when you stop typing for two seconds.\n&lt;/h4&gt;\n\n&lt;input\n  name=\"message\"\n  autocomplete=\"off\"\n  class=\"input\"\n  on:input=\"submitExpensive\"\n  on=\"submitExpensive\"\n  debounce=\"2000\"\n  src=\"/expensive\"\n  result=\"expensiveResult\"\n&gt;\n\n&lt;div render=\"expensiveResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Complete&lt;/small&gt;\n\n&lt;p&gt;Imagine that this value was hard to compute!&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;You said:&lt;/dt&gt;\n  &lt;dd&gt;{ server.getParam(\"message\") }&lt;/dd&gt;\n  &lt;dt&gt;Answer:&lt;/dt&gt;\n  &lt;dd&gt;42&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    The request is only sent when you stop typing for two seconds."},{"location":"event/on/#request-data","title":"Request Data","text":"<p>Forms submit their fields the same way they always did in HTML, including native field validation rules.</p> <p>Custom elements (e.g. Web Components) can also be made validatable; all they must do is implement the standard checkValidity method.</p> <p>What's more, any element inside the one sending the request, including the element itself, can contribute values to the request data by specifying a <code>name</code> and a <code>value</code> attribute. For non-form fields, both attributes must be provided.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;Enter your name and submit the form&lt;/h4&gt;\n\n&lt;form\n  action=\"/form-submit\"\n  on:submit=\"submitForm\"\n  on=\"submitForm\"\n  result=\"formSubmitResult\"\n&gt;\n  &lt;label&gt;\n    Name:\n    &lt;!-- no KEML-specific attributes on the input --&gt;\n    &lt;input\n      type=\"text\"\n      name=\"name\"\n      autocomplete=\"off\"\n      required\n      class=\"input\"\n    &gt;\n  &lt;/label&gt;\n\n  &lt;button class=\"btn mt2\"&gt;submit&lt;/button&gt;\n&lt;/form&gt;\n\n&lt;div render=\"formSubmitResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Received&lt;/small&gt;\n\n&lt;p&gt;\n  Form submissions may not be exciting, but where would the web be without them?\n&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Hello:&lt;/dt&gt;\n  &lt;dd&gt;{ server.getParam(\"name\") }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre> Enter your name and submit the form      Name:      submit <p>Form fields can even submit themselves without a form at all.</p> <p>This example works exactly the same.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;Enter your name and submit the field&lt;/h4&gt;\n\nName:\n&lt;!-- Look Ma, no form! --&gt;\n&lt;input\n  type=\"text\"\n  name=\"name\"\n  autocomplete=\"off\"\n  required\n  class=\"input\"\n  action=\"/form-submit\"\n  on:keydown=\"submitField\"\n  event:keydown=\"key = Enter\"\n  on=\"submitField\"\n  result=\"fieldSubmitResult\"\n&gt;\n\n&lt;button\n  type=\"button\"\n  class=\"btn mt2\"\n  on:click=\"submitField\"\n&gt;\n  submit\n&lt;/button&gt;\n\n&lt;div render=\"fieldSubmitResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Received&lt;/small&gt;\n\n&lt;p&gt;\n  Form submissions may not be exciting, but where would the web be without them?\n&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Hello:&lt;/dt&gt;\n  &lt;dd&gt;{ server.getParam(\"name\") }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre> Enter your name and submit the field  Name:     submit  <p>It does not even have to be a form field.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;A div huh \ud83e\udd14&lt;/h4&gt;\n\nName:\n&lt;div\n  name=\"name\"\n  value=\"Batman\"\n  class=\"input\"\n  action=\"/form-submit\"\n  on=\"submitDiv\"\n  result=\"divSubmitResult\"\n&gt;\n  I'm Batman!\n&lt;/div&gt;\n\n&lt;button\n  type=\"button\"\n  class=\"btn mt2\"\n  on:click=\"submitDiv\"\n&gt;\n  submit\n&lt;/button&gt;\n\n&lt;div render=\"divSubmitResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Received&lt;/small&gt;\n\n&lt;p&gt;\n  Form submissions may not be exciting, but where would the web be without them?\n&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Hello:&lt;/dt&gt;\n  &lt;dd&gt;{ server.getParam(\"name\") }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre> A div huh \ud83e\udd14  Name:    I'm Batman!     submit"},{"location":"event/on/#request-headers","title":"Request Headers","text":"<p>Request headers can be set using:</p> <p><code>h-&lt;header name&gt;=\"header value\"</code></p> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendHeaders\"\n  on=\"sendHeaders\"\n  href=\"/request-headers\"\n  h-batman=\"Bruce Wayne\"\n  result=\"headersResult\"\n&gt;\n  Send Message\n&lt;/button&gt;\n\n&lt;div render=\"headersResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Received&lt;/small&gt;\n\n&lt;p&gt;Just don't tell anyone \ud83d\ude00&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Secret identity:&lt;/dt&gt;\n  &lt;dd&gt;{ server.headers.get(\"batman\") }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    Send Message"},{"location":"event/on/#credentials","title":"credentials","text":"<p>RTFM (just kidding)</p> <p>This is simply the standard mechanism for including credentials (e.g. cookies) when making requests to another domain, where they would normally not be included for security reasons.</p> <p>Make sure you know what you're doing if you're using this.</p> <p>This is a boolean attribute, so its value is ignored and only the presence determines whether or not the credentials are enabled.</p> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendCredentials\"\n  on=\"sendCredentials\"\n  href=\"https://www.example.com/nefarious\"\n  credentials\n  result=\"credentialsResult\"\n&gt;\n  Log In\n&lt;/button&gt;\n\n&lt;div render=\"credentialsResult\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip mt3\"&gt;Ha!&lt;/small&gt;\n\n&lt;p&gt;What?!&lt;/p&gt;\n\n&lt;p&gt;You were expecting to see something, weren't you?&lt;/p&gt;\n\n&lt;p&gt;But the deed is done.&lt;/p&gt;\n\n&lt;dl class=\"dl\"&gt;\n  &lt;dt&gt;Credentials included:&lt;/dt&gt;\n  &lt;dd&gt;{ server.withCredentials ? \"Yes\" : \"No\" }&lt;/dd&gt;\n&lt;/dl&gt;\n</code></pre>    Log In"},{"location":"event/on/#redirect","title":"<code>redirect</code>","text":"<p>This attribute switches the operational mode of <code>on</code> from sending requests to performing redirects.</p> <p>The endpoint resolution logic remains the same. Headers and HTTP method are ignored. Form data (excluding file uploads) is applied to the query string.</p>"},{"location":"event/on/#assign","title":"<code>assign</code>","text":"<p>This option performs a full page navigation.</p> Page A Page B Page C Result <pre><code>&lt;a\n  href=\"/page-b\"\n  on:click=\"redirectAssignA\"\n  on=\"redirectAssignA\"\n  redirect=\"assign\"\n&gt;\n  Go to page B\n&lt;/a&gt;\n</code></pre> <pre><code>&lt;a\n  href=\"/page-c\"\n  on:click=\"redirectAssignB\"\n  on=\"redirectAssignB\"\n  redirect=\"assign\"\n&gt;\n  Go to page C\n&lt;/a&gt;\n</code></pre> <pre><code>&lt;p&gt;Congrats you have reached the last page \ud83c\udf89&lt;/p&gt;\n\n&lt;p&gt;Now use the browser buttons to go back and forth.&lt;/p&gt;\n</code></pre> https://www.assign-example.com/page-a/"},{"location":"event/on/#replace","title":"<code>replace</code>","text":"<p>This option performs a full page navigation, but replaces the current history entry instead of adding a new one.</p> Page A Page B Page C Result <pre><code>&lt;a\n  href=\"/page-b\"\n  on:click=\"redirectReplaceA\"\n  on=\"redirectReplaceA\"\n  redirect=\"assign\"\n&gt;\n  Go to page B\n&lt;/a&gt;\n</code></pre> <pre><code>&lt;a\n  href=\"/page-c\"\n  on:click=\"redirectReplaceB\"\n  on=\"redirectReplaceB\"\n  redirect=\"replace\"\n&gt;\n  Replace this page with page C\n&lt;/a&gt;\n</code></pre> <pre><code>&lt;p&gt;Congrats you have reached the last page \ud83c\udf89&lt;/p&gt;\n\n&lt;p&gt;Now use the browser buttons to go back and forth.&lt;/p&gt;\n</code></pre> https://www.replace-example.com/page-a/"},{"location":"event/on/#pushstate-and-replacestate","title":"<code>pushState</code> and <code>replaceState</code>","text":"<p>These options work exactly the same, but use the History API and do not cause a full page navigation.</p> <p>Oh, and did I forget to mention that you get SSR for free? It is not some separate thing you have to implement, and it does not impose restrictions on your server infrastructure \ud83e\udd2f.</p> HTML Page A Page B Page C Result <pre><code>&lt;a\n  href=\"/page-a\"\n  class=\"mr3\"\n  on:click=\"redirectPushStateA\"\n  on=\"redirectPushStateA\"\n  redirect=\"pushState\"\n&gt;\n  Push A\n&lt;/a&gt;\n\n&lt;a\n  href=\"/page-b\"\n  class=\"mr3\"\n  on:click=\"redirectPushStateB\"\n  on=\"redirectPushStateB\"\n  redirect=\"pushState\"\n&gt;\n  Push B\n&lt;/a&gt;\n\n&lt;a\n  href=\"/page-c\"\n  class=\"mr3\"\n  on:click=\"redirectPushStateC\"\n  on=\"redirectPushStateC\"\n  redirect=\"pushState\"\n&gt;\n  Push C\n&lt;/a&gt;\n|\n&lt;a\n  href=\"/page-a\"\n  class=\"mh3\"\n  on:click=\"redirectReplaceStateA\"\n  on=\"redirectReplaceStateA\"\n  redirect=\"replaceState\"\n&gt;\n  Replace A\n&lt;/a&gt;\n\n&lt;a\n  href=\"/page-b\"\n  class=\"mr3\"\n  on:click=\"redirectReplaceStateB\"\n  on=\"redirectReplaceStateB\"\n  redirect=\"replaceState\"\n&gt;\n  Replace B\n&lt;/a&gt;\n\n&lt;a\n  href=\"/page-c\"\n  on:click=\"redirectReplaceStateC\"\n  on=\"redirectReplaceStateC\"\n  redirect=\"replaceState\"\n&gt;\n  Replace C\n&lt;/a&gt;\n\n&lt;div\n  on:navigate=\"showHistoryPage\"\n  on=\"showHistoryPage\"\n  result=\"historyResult\"\n  render=\"historyResult\"\n&gt;\n  { server.partial } &lt;!-- SSR partial render --&gt;\n&lt;/div&gt;\n</code></pre> <pre><code>&lt;p&gt;The A page content.&lt;/p&gt;\n</code></pre> <pre><code>&lt;p&gt;The B page content.&lt;/p&gt;\n</code></pre> <pre><code>&lt;p&gt;The C page content.&lt;/p&gt;\n</code></pre> https://www.history-example.com/page-a/"},{"location":"event/on/#once","title":"<code>once</code>","text":"<p>This is a self-destruct instruction for the <code>on</code> attribute. It is removed after the first invocation.</p> HTML Server Result <pre><code>&lt;h4 class=\"mt0\"&gt;You can only add a single entry to the log.&lt;/h4&gt;\n\n&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendOnce\"\n  on=\"sendOnce\"\n  href=\"/once\"\n  once\n  result=\"onceResult\"\n&gt;\n  Append Log\n&lt;/button&gt;\n\n&lt;ul\n  class=\"mv0\"\n  render=\"onceResult\"\n  position=\"append\"\n&gt;&lt;/ul&gt;\n</code></pre> <pre><code>&lt;li class=\"mt3\"&gt;Log entry from: { new Date().toLocaleString() }&lt;/li&gt;\n</code></pre> You can only add a single entry to the log.    Append Log"},{"location":"event/on/#polling","title":"Polling","text":"<p>Info</p> <p>Example only provided for completeness. You are discouraged from doing this. Use SSE if possible.</p> <p>KEML does not implement polling as a dedicated feature, but it can still be assembled from the basic building blocks shown above.</p> Server Result <pre><code>{{ const colors = [\"red\", \"orange\", \"gold\", \"yellow\", \"purple\", \"pink\", \"green\",\n  \"navy\", \"blue\"]; }}\n\n&lt;div\n  class=\"w3 h3 bg-{ colors[Math.random() * colors.length | 0] }\"\n  on:discover=\"startPolling\"\n  on=\"startPolling\"\n  debounce=\"5000\"\n  get=\"/polling\"\n  on:result=\"startPolling\"\n  result=\"pollingResult\"\n  render=\"pollingResult\"\n  position=\"replaceWith\"\n&gt;&lt;/div&gt;\n</code></pre> Changes color every 5 seconds."},{"location":"event/reset/","title":"reset","text":"<p>Like on, <code>reset</code> also accepts a single event action as input.</p> <p>The only job of this feature is to restore the element's default value.</p> <p>Custom elements (e.g. Web Components) can also be made resettable; all they must do is implement the standard reset method.</p> HTML Result <pre><code>&lt;h4 class=\"mt0\"&gt;\n  Change the value of the input and click the button to restore it.\n&lt;/h4&gt;\n\n&lt;form reset=\"doReset\"&gt;\n  &lt;input\n    type=\"text\"\n    value=\"edit me\"\n    class=\"input\"\n  &gt;\n&lt;/form&gt;\n\n&lt;button\n  type=\"button\"\n  class=\"btn mt2\"\n  on:click=\"doReset\"\n&gt;\n  click me\n&lt;/button&gt;\n</code></pre>    Change the value of the input and click the button to restore it.     click me"},{"location":"event/scroll/","title":"scroll","text":"<p>Like on, <code>scroll</code> also accepts a single event action as input.</p> <p>This feature scrolls the element.</p> <p>Custom elements (e.g. Web Components) can also be made scrollable; all they must do is implement the standard scroll and scrollBy methods.</p> <p><code>scroll</code> provides a set of optional attributes for customizing its behavior. These are covered in the sections below.</p>"},{"location":"event/scroll/#behavior","title":"<code>behavior</code>","text":"<p>Warning</p> <p>Smooth scrolling is disabled because of your system\u2019s motion settings.</p> <p>You can enable it here:</p> <ul> <li>Windows: Settings \u2192 Accessibility \u2192 Visual effects \u2192 Animation effects</li> <li>macOS: System Settings \u2192 Accessibility \u2192 Display \u2192 Reduce motion</li> <li>iOS: Settings \u2192 Accessibility \u2192 Motion \u2192 Reduce Motion</li> <li>Android: Settings \u2192 System \u2192 Accessibility \u2192 Remove animations</li> <li>Firefox: <code>about:config</code> -&gt; add <code>ui.prefersReducedMotion</code> (number) and   set it to <code>0</code> or <code>1</code>. This overrides the system setting.</li> </ul> <p>Specifies whether the scrolling should animate smoothly (<code>smooth</code>), happen instantly in a single jump (<code>instant</code>), or let the browser choose (<code>auto</code>, default).</p> HTML Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn mb3\"\n  on:click=\"scrollBehavior\"\n&gt;\n  scroll down\n&lt;/button&gt;\n\n&lt;div\n  class=\"overflow-y-auto h4\"\n  scroll=\"scrollBehavior\"\n  behavior=\"smooth\"\n  top=\"500\"\n&gt;\n  &lt;div class=\"h5 bg-red\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-green\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-blue\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-yellow\"&gt;&lt;/div&gt;\n&lt;/div&gt;\n</code></pre>    scroll down"},{"location":"event/scroll/#relative","title":"<code>relative</code>","text":"<p>This is a boolean attribute. When present, scrolling uses relative offsets instead of an absolute position.</p> HTML Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn mb3\"\n  on:click=\"scrollRelative\"\n&gt;\n  scroll down\n&lt;/button&gt;\n\n&lt;div\n  class=\"overflow-y-auto h4\"\n  scroll=\"scrollRelative\"\n  relative\n  top=\"200\"\n&gt;\n  &lt;div class=\"h5 bg-red\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-green\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-blue\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-yellow\"&gt;&lt;/div&gt;\n&lt;/div&gt;\n</code></pre>    scroll down"},{"location":"event/scroll/#top","title":"<code>top</code>","text":"<p>Specifies how much to scroll vertically.</p> <p>Can be set to one of the following:</p> <ul> <li>a numeric pixel value</li> <li><code>start</code> to scroll all the way to the top</li> <li><code>center</code> to scroll to the middle</li> <li><code>end</code> to scroll all the way to the bottom</li> </ul> HTML Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn mb3\"\n  on:click=\"scrollTop\"\n&gt;\n  scroll down\n&lt;/button&gt;\n\n&lt;div\n  class=\"overflow-y-auto h4\"\n  scroll=\"scrollTop\"\n  top=\"end\"\n&gt;\n  &lt;div class=\"h5 bg-red\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-green\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-blue\"&gt;&lt;/div&gt;\n  &lt;div class=\"h5 bg-yellow\"&gt;&lt;/div&gt;\n&lt;/div&gt;\n</code></pre>    scroll down"},{"location":"event/scroll/#left","title":"<code>left</code>","text":"<p>Specifies how much to scroll horizontally.</p> <p>Can be set to one of the following:</p> <ul> <li>a numeric pixel value</li> <li><code>start</code> to scroll all the way to the left</li> <li><code>center</code> to scroll to the middle</li> <li><code>end</code> to scroll all the way to the right</li> </ul> HTML Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn mb3\"\n  on:click=\"scrollLeft\"\n&gt;\n  scroll right\n&lt;/button&gt;\n\n&lt;div\n  class=\"overflow-x-auto nowrap h4\"\n  scroll=\"scrollLeft\"\n  left=\"center\"\n&gt;\n  &lt;div class=\"h3 w5 dib bg-red\"&gt;&lt;/div&gt;\n  &lt;div class=\"h3 w5 dib bg-green\"&gt;&lt;/div&gt;\n  &lt;div class=\"h3 w5 dib bg-blue\"&gt;&lt;/div&gt;\n  &lt;div class=\"h3 w5 dib bg-yellow\"&gt;&lt;/div&gt;\n&lt;/div&gt;\n</code></pre>    scroll right"},{"location":"result/render/","title":"render","text":"<p>Just like on is the input channel for event actions, so too is the <code>render</code> attribute the input channel for result actions.</p> <p>This attribute can appear on any number of elements - if they all take the same result action, they will all render the same server result.</p> <p>Rendering is always a granular operation, applying the smallest possible number of DOM mutations. If the server result already matches the current state of the DOM, no changes are applied at all.</p>"},{"location":"result/render/#position","title":"<code>position</code>","text":"<p>This attribute controls how the server result is applied to the element.</p> <p>It can be set to one of the following values:</p> <ul> <li><code>replaceChildren</code> (default)</li> <li><code>replaceWith</code></li> <li><code>before</code></li> <li><code>after</code></li> <li><code>prepend</code></li> <li><code>append</code></li> </ul> <p>All of these are logically equivalent to the DOM methods with the same names, even if mechanically not identical.</p> HTML Server Result <pre><code>&lt;div\n  render=\"positionResult\"\n  position=\"replaceWith\"\n&gt;\n  &lt;h4&gt;Click the button to replace it with a cat picture.&lt;/h4&gt;\n\n  &lt;button\n    type=\"button\"\n    class=\"btn\"\n    on:click=\"sendPosition\"\n    on=\"sendPosition\"\n    href=\"/position\"\n    result=\"positionResult\"\n  &gt;\n    hey kitty kitty\n  &lt;/button&gt;\n&lt;/div&gt;\n</code></pre> <pre><code>&lt;img src=\"../../assets/cat.jpg\" alt=\"cat\"&gt;\n\n&lt;p class=\"mb0\"&gt;Bazinga \ud83d\ude05&lt;/p&gt;\n</code></pre> Click the button to replace it with a cat picture.      hey kitty kitty"},{"location":"result/render/#key","title":"<code>key</code>","text":"<p>This attribute serves the same function as it does in frameworks such as ReactJS: it is a hint to the DOM patcher that helps it distinguish between otherwise similar-looking elements. In KEML, however, it is less restrictive and is never strictly required.</p> <p>There is no such thing as a free lunch, though. Everything a computer does has some cost associated with it \u2014 some costs are just more desirable than others.</p> <p><code>key</code> is also not a silver bullet or a general performance optimization applicable in every situation.</p> <p>Rules of thumb:</p> <ul> <li>Use it only for same-tag siblings that can be reordered, inserted, or removed.</li> <li>Do not use it to represent text or attribute differences.</li> <li>Treat it as stable identity, not as data.</li> </ul> Server Result <pre><code>&lt;div\n  render=\"keyResult\"\n  position=\"replaceWith\"\n&gt;\n  &lt;button\n    type=\"button\"\n    class=\"btn mb3\"\n    on:click=\"toggleKey\"\n    on=\"toggleKey\"\n    href=\"/key\"\n    name=\"switch\"\n    value=\"{ server.getParam('switch') === 'hide' ? 'show' : 'hide' }\"\n    result=\"keyResult\"\n  &gt;\n    { server.getParam(\"switch\") } notification\n  &lt;/button&gt;\n\n  &lt;div&gt;\n    {{ if (server.getParam(\"switch\") === \"hide\") { }}\n      &lt;div key=\"notification\" class=\"admonition info mt0\"&gt;\n        &lt;p class=\"admonition-title\"&gt;Info&lt;/p&gt;\n        &lt;p&gt;Just passing by...&lt;/p&gt;\n      &lt;/div&gt;\n    {{ } }}\n\n    &lt;div&gt;\n      &lt;dl class=\"dl\"&gt;\n        &lt;dt&gt;Roses are&lt;/dt&gt;\n        &lt;dd&gt;red&lt;/dd&gt;\n        &lt;dt&gt;Violets are&lt;/dt&gt;\n        &lt;dd&gt;blue&lt;/dd&gt;\n        &lt;dt&gt;KEML is&lt;/dt&gt;\n        &lt;dd&gt;awesome&lt;/dd&gt;\n        &lt;dt&gt;And so are&lt;/dt&gt;\n        &lt;dd&gt;you&lt;/dd&gt;\n      &lt;/dl&gt;\n    &lt;/div&gt;\n  &lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>   Both the notification message and the definition list are wrapped in their own   <code>div</code> elements. </p> <p>   Because the notification identifies itself using the <code>key</code>   attribute, the definition list is never disturbed. </p>      show notification    Roses are red Violets are blue KEML is awesome And so are you"},{"location":"result/result/","title":"result / error","text":"<p>Just like on:* is the output channel for event actions, so too are the <code>result</code> and <code>error</code> attributes the output channels for result actions.</p> <p>Result actions can be defined on any element that can receive server results. This means that the element is either sending requests to the server, or is directly pushed results into by the server (e.g. SSE).</p> <p>They are defined like so:</p> <ul> <li><code>result=\"all good\"</code> \u2192 actions generated by successful status codes</li> <li><code>error=\"something amiss\"</code> \u2192 actions generated by unsuccessful status codes</li> </ul> <p>They are identical in every way and only filter by the response status code.</p> <p>The elements that receive server results, regardless of whether or not they have <code>result</code> and/or <code>error</code> attributes, will emit events on themselves upon receipt of those results:</p> <ul> <li><code>result</code> \u2192 successful status code</li> <li><code>failure</code> \u2192 unsuccessful status code (if you're wondering why not <code>error</code>, the   reason is simply because <code>error</code> is already an existing reserved browser event   type)</li> </ul>"},{"location":"result/result/#result","title":"<code>result</code>","text":"<p>This demo shows a server sending a response with a successful status code.</p> <p>When it is received:</p> <ol> <li>the response is displayed</li> <li>a secondary request is triggered using the <code>result</code> event, and its response    is also displayed</li> </ol> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendResultSuccessOne\"\n  on=\"sendResultSuccessOne\"\n  href=\"/result-success\"\n  result=\"resultSuccessOne\"\n  on:result=\"sendResultSuccessTwo\"\n&gt;\n  Send\n&lt;/button&gt;\n\n&lt;input\n  type=\"hidden\"\n  name=\"cantSeeMe\"\n  on=\"sendResultSuccessTwo\"\n  href=\"/result-success\"\n  result=\"resultSuccessTwo\"\n&gt;\n\n&lt;div render=\"resultSuccessOne\"&gt;&lt;/div&gt;\n\n&lt;div render=\"resultSuccessTwo\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>&lt;small class=\"chip success mt3\"&gt;Hurray&lt;/small&gt;\n\n&lt;p class=\"mb0\"&gt;Everything is awesome \ud83c\udf89&lt;/p&gt;\n</code></pre>    Send"},{"location":"result/result/#error","title":"<code>error</code>","text":"<p>This demo shows a server sending a response with an unsuccessful status code.</p> <p>When it is received:</p> <ol> <li>the response is displayed</li> <li>a secondary request is triggered using the <code>failure</code> event, and its response    is also displayed</li> </ol> HTML Server Result <pre><code>&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendResultFailureOne\"\n  on=\"sendResultFailureOne\"\n  href=\"/result-failure\"\n  error=\"resultFailureOne\"\n  on:failure=\"sendResultFailureTwo\"\n&gt;\n  Send\n&lt;/button&gt;\n\n&lt;input\n  type=\"hidden\"\n  name=\"cantSeeMe\"\n  on=\"sendResultFailureTwo\"\n  href=\"/result-failure\"\n  error=\"resultFailureTwo\"\n&gt;\n\n&lt;div render=\"resultFailureOne\"&gt;&lt;/div&gt;\n\n&lt;div render=\"resultFailureTwo\"&gt;&lt;/div&gt;\n</code></pre> <pre><code>{{ server.status = 500 }}\n\n&lt;small class=\"chip error mt3\"&gt;Oh, no!&lt;/small&gt;\n\n&lt;p class=\"mb0\"&gt;An error has occurred \ud83e\udd37&lt;/p&gt;\n</code></pre>    Send"},{"location":"state/if-colon/","title":"if:*","text":"<p>Just like on:* is the output channel for event actions, so too is the <code>if:*</code> attribute the output channel for state actions.</p> <p>All of the following, along with any future additions, are behaviorally identical and differ only in how their conditions are calculated.</p>"},{"location":"state/if-colon/#ifloading","title":"<code>if:loading</code>","text":"<p>KEML has no special concept of a loading indicator, because it does not need one \u2014 <code>loading</code> is just another state condition.</p> HTML Result <pre><code>&lt;h4 class=\"mt0\"&gt;This endpoint takes 2 seconds to respond.&lt;/h4&gt;\n\n&lt;button\n  type=\"button\"\n  class=\"btn\"\n  on:click=\"sendIfLoading\"\n  on=\"sendIfLoading\"\n  href=\"/if-loading\"\n  if:loading=\"isLoading\"\n&gt;\n  click me\n&lt;/button&gt;\n\n&lt;div\n  if=\"isLoading\"\n  x-class=\"spinner mt3\"\n&gt;&lt;/div&gt;\n</code></pre> This endpoint takes 2 seconds to respond.    click me"},{"location":"state/if-colon/#iferror","title":"<code>if:error</code>","text":"<p>The <code>error</code> condition becomes <code>true</code> when the server returns an unsuccessful status code.</p> HTML Server Result <pre><code>&lt;button\n  class=\"btn mr3\"\n  on:click=\"sendIfError\"\n  on=\"sendIfError\"\n  href=\"/if-error\"\n  name=\"switch\"\n  value=\"success\"\n  result=\"ifErrorResult\"\n  error=\"ifErrorResult\"\n  render=\"ifErrorResult\"\n  position=\"replaceWith\"\n  if:error=\"isError\"\n&gt;\n  Get error code\n&lt;/button&gt;\n\n&lt;small\n  if=\"isError\"\n  class=\"dn\"\n  x-class=\"chip error\"\n&gt;\n  Error\n&lt;/small&gt;\n\n&lt;small\n  if=\"isError\"\n  class=\"chip success\"\n  x-class=\"dn\"\n&gt;\n  No errors\n&lt;/small&gt;\n</code></pre> <pre><code>{{ server.status = server.getParam('switch') === 'error' ? 200 : 500 }}\n\n&lt;button\n  class=\"btn mr3\"\n  on:click=\"sendIfError\"\n  on=\"sendIfError\"\n  href=\"/if-error\"\n  name=\"switch\"\n  value=\"{ server.getParam('switch') === 'error' ? 'success' : 'error' }\"\n  result=\"ifErrorResult\"\n  error=\"ifErrorResult\"\n  render=\"ifErrorResult\"\n  position=\"replaceWith\"\n  if:error=\"isError\"\n&gt;\n  Get { server.getParam(\"switch\") } code\n&lt;/button&gt;\n</code></pre>    Get error code     Error     No errors"},{"location":"state/if-colon/#ifinvalid","title":"<code>if:invalid</code>","text":"<p>The <code>invalid</code> condition becomes <code>true</code> when the element's value is invalid.</p> <p>For custom elements (e.g. Web Components) to participate, they must implement the standard checkValidity method and notify the system of their value changes by emitting one or more of the following event types: <code>change</code>, <code>input</code>, <code>reset</code>.</p> HTML Result <pre><code>&lt;input\n  type=\"email\"\n  class=\"input\"\n  placeholder=\"Email address\"\n  if:invalid=\"isInvalidEmail\"\n&gt;\n\n&lt;small\n  if=\"isInvalidEmail\"\n  class=\"dn\"\n  x-class\n&gt;\n  Please enter a valid email address.\n&lt;/small&gt;\n</code></pre>    Please enter a valid email address."},{"location":"state/if-colon/#ifvalue","title":"<code>if:value</code>","text":"<p>The <code>value</code> condition becomes <code>true</code> when the element's value is truthy.</p> <p>Custom elements (like Web Components) can participate if they follow the same input-like behavior as native input elements:</p> <ul> <li>They must expose a <code>type</code> property.</li> <li>They may expose additional input-like properties depending on <code>type</code>:   <code>checked</code>, <code>files</code>, <code>src</code>, and <code>value</code>.</li> <li>When their value changes, they notify the system using one or more of these   events: <code>input</code>, <code>change</code>, <code>reset</code>.</li> </ul> HTML Result <pre><code>&lt;input\n  type=\"text\"\n  class=\"input\"\n  placeholder=\"Say something...\"\n  if:value=\"isValue\"\n&gt;\n\n&lt;small\n  if=\"isValue\"\n  class=\"dn\"\n  x-class\n&gt;\n  The input is not empty.\n&lt;/small&gt;\n</code></pre>    The input is not empty."},{"location":"state/if-colon/#ifintersects","title":"<code>if:intersects</code>","text":"<p>The <code>intersects</code> condition becomes <code>true</code> when the element enters the viewport.</p> HTML Result <pre><code>&lt;p\n  if=\"isIntersecting\"\n  class=\"mt0\"\n  x-class=\"mt0 green\"\n&gt;\n  Scroll the container down to reveal the green &lt;code&gt;div&lt;/code&gt;, and watch this\n  paragraph turn green as well.\n&lt;/p&gt;\n\n&lt;div class=\"overflow-y-auto h4\"&gt;\n  &lt;div class=\"h5\"&gt;&lt;/div&gt;\n\n  &lt;div\n    class=\"h5 bg-green\"\n    if:intersects=\"isIntersecting\"\n  &gt;&lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>   Scroll the container down to reveal the green <code>div</code>, and watch this   paragraph turn green as well. </p>"},{"location":"state/if/","title":"if","text":"<p>Just like on is the input channel for event actions, so too is the <code>if</code> attribute the input channel for state actions.</p> <p>All of the usage examples are already shown on the if:* page. This page only clarifies what those examples demonstrate.</p> <p>The <code>if</code> attribute allows elements to switch between two possible configurations of their attributes - one configuration for when a state condition is <code>true</code> and another for when it is <code>false</code>.</p> <p>Any attribute with a name starting with <code>x-</code> will trade places with its unprefixed counterpart while the condition is <code>true</code>, and will be restored back when it is <code>false</code>. If a counterpart does not exist, its non-existence will also be restored.</p> <p>Isn't it great when things are simple and all features work the same? \ud83e\udd2f</p>"}]}